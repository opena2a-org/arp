{
  "name": "@opena2a/arp",
  "version": "0.1.0",
  "description": "Agent Runtime Protection — LLM-assisted behavioral monitoring, anomaly detection, and enforcement for AI agents.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "arp-guard": "dist/cli/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "js-yaml": "^4.1.1"
  },
  "peerDependencies": {
    "@opena2a/aim-core": ">=0.1.0"
  },
  "peerDependenciesMeta": {
    "@opena2a/aim-core": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.3",
    "vitest": "^3.0.0"
  },
  "keywords": [
    "ai",
    "agent",
    "security",
    "runtime",
    "protection",
    "monitoring",
    "edr",
    "opena2a"
  ],
  "author": "OpenA2A",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
